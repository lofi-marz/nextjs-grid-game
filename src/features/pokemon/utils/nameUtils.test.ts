import { describe, it, expect } from 'vitest';

describe('Name Utils', () => {
    it('Should correctly parse pokemon with just name', () => {
        const pokemonName = 'bulbasaur';
    });

    it('Should correctly parse pokemon with name and variant', () => {});

    it('Should correctly parse pokemon with name and region', () => {});

    it('Should correctly parse pokemon with name, region and variant', () => {});
});
